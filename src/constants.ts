export enum ROUTES {
	HOME = '/',
    NOTE = '/note',
	NOTE_CREATE = '/note/create',
};

export enum BUTTON_TYPES {
    Button = "button",
    Submit = "submit",
    Reset = "reset",
};

export enum BUTTON_COLORS {
    Green = "green",
    Red = "red",
};
